<div class='container p-3 col-3 mt-3 mb-5 mb-5 p-3 bg-white p-5 border border-info rounded-4'>
  <div class='row'>
    <div class='col'>
      <div >
        <h2 class="d-flex justify-content-center mt-5"><%= @practice.name  %></h2>
        <p class="d-flex justify-content-center mt-5" ><%= @practice.info %></p>
        <p class="d-flex justify-content-center"><%= link_to 'search', practice_institutions_path(@practice), class:"userList mt-5 btn btn-primary"%></p>
        <p class="text-decoration-none d-flex justify-content-center mt-5"><%= link_to 'create card', new_practice_card_path(@practice), class:"userList btn btn-primary" %></p>
        <p class="text-decoration-none d-flex justify-content-center mt-5"><%= link_to 'back', root_path, class:"userList btn btn-outline-danger" %></p>
      </div>
    </div>
  </div>
</div>
